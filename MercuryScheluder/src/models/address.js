export default class Address {
    constructor(city, street, postalCode) {
        this.city = city;
        this.street = street;
        this.postalCode = postalCode;
    }

    // get City() { return this.city; }
    // set City(city) { this.city = city; }
    
    // get Street() { return this.street; }
    // set Street(street) { this.street = street }
    
    // get PostalCode() { return this.postalCode }
    // set PostalCode(postalCode) { this.postalCode = postalCode }
}